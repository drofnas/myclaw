# TOOLS.md - Local Notes

Skills define _how_ tools work. This file is for _your_ specifics — the stuff unique to this setup.

## What Goes Here

- SSH hosts and aliases
- Repo locations and remotes
- Local service URLs (dev servers, DBs, etc.)
- Build commands or shortcuts that aren't obvious
- Environment quirks worth knowing

## Examples

```markdown
### Repos
- myclaw → ~/Projects/Personal/myclaw
- openclaw → ~/Projects/Personal/_download/openclaw (pnpm, bun)

### Dev servers
- API → http://localhost:3000
- Docs → http://localhost:3333

### SSH
- home-server → 192.168.1.100, user: admin
```

---

_(Fill this in as you learn the setup. The right notes here save a lot of "wait, where's the config?" later.)_
